/// Q64.64 fixed-point sqrt price
pub type SqrtPrice = u128;

/// Liquidity amount (L in formulas)
pub type Liquidity = u128;

/// Tick index (-443636 to +443636)
pub type TickIndex = i32;

/// Fee rate in hundredths of a basis point
pub type FeeRate = u16;

/// Unix timestamp
pub type Timestamp = i64;
